# Document Versioning API Configuration
# This file can be overridden by environment variables

server:
  port: 8080
  host: "0.0.0.0"

database:
  host: "localhost"
  port: 5432
  user: "postgres"
  password: "postgres"
  name: "docstore"
  sslmode: "disable"
